about <- function(input, output, session) {
  
  # Display Session Info
  # output$sessionInfo <- renderPrint({
  #   # capture.output(sessionInfo())
  #   sessionInfo()
  # })
  # observeEvent(input$sessionButton, {
  #   toggle('session_text')
  #   output$sessionInfo <- renderText({capture.output(sessionInfo())})
  # })
  # observeEvent(input$button, {
  #   toggle(ns('text_div'))
  #   # output$text <- renderText({ns("sessionInfo")})
  #   output$text <- renderText(ns("sessionInfo"))
  # })
}

